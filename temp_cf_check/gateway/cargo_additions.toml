# Add these dependencies to your sx9-gateway/Cargo.toml

[dependencies]
# Existing dependencies...
# axum = "0.7"
# tokio = { version = "1", features = ["full"] }
# serde = { version = "1", features = ["derive"] }
# serde_json = "1"
# tracing = "0.1"
# anyhow = "1"

# R2 / S3 Client Dependencies
aws-config = "1.1.0"
aws-sdk-s3 = "1.9.0"
aws-smithy-runtime-api = "1.1.0"

# Time handling
chrono = { version = "0.4", features = ["serde"] }

# Optional features
[features]
default = []
r2-cdn = ["aws-sdk-s3", "aws-config"]
