export const config = {
  boltNewUrl: import.meta.env.VITE_BOLT_NEW_URL || 'https://bolt.new',
  wolframAppId: import.meta.env.VITE_WOLFRAM_APP_ID,
  openAiApiKey: import.meta.env.VITE_OPENAI_API_KEY,
  // Add other configuration variables as needed
};