# SX9 Linear Agent Configuration
# Keys loaded from environment (source tools/vault/setup-api-keys.sh)

# Linear API Key - loaded from $LINEAR_API_KEY
linear_api_key = "${LINEAR_API_KEY}"

# Slack Bot Token - loaded from $SLACK_BOT_TOKEN
slack_bot_token = "${SLACK_BOT_TOKEN}"

# Serena MCP Endpoint
serena_endpoint = "http://localhost:8000"

# Linear Team ID (SX9)
team_id = "5f2c1e8a-3b4d-4c9a-8e1f-2d3c4b5a6e7f"

# Linear Project ID (optional, will create if not specified)
project_id = ""

# Slack Channel for Notifications
slack_channel = "#sx9-dev"

# Agent Settings
[agent]
# Poll interval for checking Linear issues (seconds)
poll_interval = 60

# Maximum concurrent tasks
max_concurrent_tasks = 3

# Auto-assign issues to agent
auto_assign = true

# Security Settings
[security]
# Allowed commands (whitelist)
allowed_commands = [
    "cargo",
    "git",
    "npm",
    "pnpm",
    "rustc",
    "rustfmt",
    "clippy"
]

# Allowed file paths (relative to project root)
allowed_paths = [
    "src/",
    "crates/",
    "tests/",
    "Cargo.toml",
    "package.json"
]

# Forbidden patterns
forbidden_patterns = [
    "rm -rf",
    "sudo",
    "/etc/",
    "/var/",
    "~/.ssh"
]

# QA Gates
[qa]
# Enable static analysis
enable_static_gate = true

# Enable architecture compliance
enable_arch_gate = true

# Enable pattern matching
enable_pattern_gate = true

# Minimum quality score (0.0-1.0)
min_quality_score = 0.7
